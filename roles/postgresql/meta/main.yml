---
galaxy_info:
  role_name: postgresql
  author: dify-pg-ha
  description: Install PostgreSQL 16 on HA nodes (Patroni manages config)
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
dependencies:
  - role: os_prep
