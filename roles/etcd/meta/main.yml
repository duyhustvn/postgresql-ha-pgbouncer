---
galaxy_info:
  role_name: etcd
  author: postgres-ha
  description: Deploy etcd cluster as DCS for Patroni
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
dependencies:
  - role: os_prep
